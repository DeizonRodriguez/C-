#include <iostream>

using namespace std;

int main() {
    int n
    cin >> n;
    if (n%2==0)
        n=n+1;

    char arrN[n][n];
    int prob;

    prob = rand()%4
    arrN[(n-1/2)][(n-1/2)]=".";
    return 0;
}
